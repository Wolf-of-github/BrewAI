# Secrets and Terraform state — never commit
**/*.tfvars
**/*.tfstate
**/*.tfstate.backup
**/terraform.tfstate*
**/.terraform/
**/.terraform.lock.hcl

# Dependency directories
node_modules/

# Python virtual environments
.venv/
venv/

# Local / OS
.DS_Store
data
commands.txt
help.txt

# Internal tools — not part of open source release
scripts
meta-data
admin-monitor

# IDE / tooling
.claude